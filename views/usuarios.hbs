<video autoplay muted loop id="videoFondo">
    <source src="/videos/video_fondo.mp4" type="video/mp4">
</video>

<h1 style="color:white; text-align:center; margin-top:40px;">Usuarios y sus canciones</h1>

<div class="d-flex justify-content-center mb-3">
    <a href="/index" class="btn btn-primary">⬅ Regresar</a>
</div>

<div class="d-flex flex-column align-items-center gap-4 mt-2">
    {{#each usuarios}}
        <div class="card shadow-lg" style="width:350px; background: rgba(0,0,0,0.7); color:white; border:1px solid #fff; border-radius:10px;">
            <div class="card-body">
                <h5 class="card-title">{{@key}}</h5>
                <ul>
                    {{#each this}}
                        <li style="font-size:0.85rem;">
                            🎵 <strong>{{nombre}}</strong> | Álbum: {{album}} | Fecha: {{fecha}}
                        </li>
                    {{/each}}
                </ul>
            </div>
        </div>
    {{/each}}
</div>
